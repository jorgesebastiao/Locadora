version: "3.7"

services:
  locadora:
    image: mehiell/locadora
    build:
      context: ./src
      dockerfile: Locadora.WebApi/Dockerfile
    container_name: locadora
    environment:
      - ASPNETCORE_URLS=http://+
    ports:
      - "80:80"   
